import React from 'react';

const Uncle = (props) => {
     const { house } = props;
     return (
          <div className="father">

               <h1>unlce house {house}</h1>

          </div>
     );
};

export default Uncle;